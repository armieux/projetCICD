// testData.js
const userData = {
    firstname: 'John',
    lastname: 'Doe',
    email: 'john.doe@example.com',
    // add other required fields if necessary
};

const groupData = {
    name: 'Test Group',
    // add other required fields if necessary
};

module.exports = { userData, groupData };
